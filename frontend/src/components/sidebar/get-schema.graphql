query GetSchema($parent: SourceObjectRefInput, $kinds: [SourceObjectKind!]) {
    Schema: SourceObjects(parent: $parent, kinds: $kinds) {
        Name
        Kind
        Ref {
            Kind
            Locator
            Path
        }
    }
}
