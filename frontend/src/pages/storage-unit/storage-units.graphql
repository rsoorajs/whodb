query GetStorageUnits($parent: SourceObjectRefInput) {
  StorageUnit: SourceObjects(parent: $parent) {
    Ref {
      Kind
      Path
    }
    Kind
    Name
    Attributes: Metadata {
      Key
      Value
    }
    Actions
    HasChildren
  }
}
