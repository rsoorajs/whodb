query GetColumns($ref: SourceObjectRefInput!) {
  Columns: SourceColumns(ref: $ref) {
    Name
    Type
    IsPrimary
    IsForeignKey
    ReferencedTable
    ReferencedColumn
    Length
    Precision
    Scale
  }
}
