mutation ImportPreview(
  $file: Upload!
  $options: ImportFileOptions!
  $ref: SourceObjectRefInput
  $useHeaderMapping: Boolean
) {
  ImportPreview(
    file: $file
    options: $options
    ref: $ref
    useHeaderMapping: $useHeaderMapping
  ) {
    Sheet
    Columns
    Rows
    Truncated
    ValidationError
    RequiresAllowAutoGenerated
    AutoGeneratedColumns
    Mapping {
      SourceColumn
      TargetColumn
    }
  }
}
