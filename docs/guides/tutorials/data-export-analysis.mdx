---
title: "Data Export Analysis"
description: "Filter, review, and export data with current WhoDB export options"
---

# Data Export Analysis

This tutorial shows the current export workflow.

## 1. Open The Data Grid

Open **Storage Units**, then click **Data** for the object you want to export.

![Data View Users Table](/images/09-data-view-users-table.png)

## 2. Narrow The Dataset

For server-side filtering:

1. Click **Add** beside **Where condition**.
2. Choose field, operator, and value.
3. Add the condition.
4. Click **Query**.

![Where Conditions Popover](/images/16-data-view-where-conditions-popover.png)

![Multiple Conditions](/images/39-data-view-multiple-conditions.png)

For quick visible-row lookup, use Search. For exports, apply WHERE conditions first so the grid contains the dataset you want.

## 3. Export All Filtered Rows

After the grid shows the data you want, click **Export All**.

![Export Dialog](/images/20-data-view-export-dialog.png)

Choose a current CE format:

- CSV.
- Excel `.xlsx`.
- JSON Lines for records from sources like MongoDB when supported.

![Export Format CSV Option](/images/65-export-format-csv-option.png)

![Export Format Excel Option](/images/66-export-format-excel-option.png)

CSV delimiter options are comma, semicolon, and pipe.

![Export Delimiter Comma](/images/67-export-delimiter-comma.png)

## 4. Export Selected Rows

Select rows with checkboxes or table shortcuts, then use the selected-row export action.

![Table Row Selection Single](/images/44-table-row-selection-single.png)

![Export Selected Rows Dialog](/images/46-export-selected-rows-dialog.png)

The export starts from the rows you selected in the grid.

## Backups And Migration Dumps

Use database-native tools when you need a full backup, SQL dump, or migration archive:

```bash
pg_dump --format custom --file backup.dump "$DATABASE_URL"
```

## Verify Downloads

Use local tools after download:

```bash
ls -lh downloads/
head -n 5 downloads/export.csv
wc -l downloads/export.csv
```

Adjust the path and filename to match your browser download location.

## Related Pages

<CardGroup cols={2}>
<Card title="Export Options" icon="file-export" href="/advanced/export-options">
See supported formats and entry points.
</Card>
<Card title="WHERE Conditions" icon="filter" href="/advanced/where-conditions">
Filter before export.
</Card>
</CardGroup>
