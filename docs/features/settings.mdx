---
title: "Settings"
description: "Current settings and personalization options in WhoDB"
---

# Settings

The current CE build includes a dedicated settings page. It is enabled in the default CE feature flags and covers telemetry, UI personalization, table defaults, language, and cloud-provider management.

## Settings Sections

### Telemetry

The settings page includes a telemetry toggle that lets you opt in or out of client analytics collection.

### Personalization

Current appearance and interaction settings include:

- storage unit view: `list` or `card`
- font size: `small`, `medium`, or `large`
- border radius: `none`, `small`, `medium`, or `large`
- spacing: `compact`, `comfortable`, or `spacious`
- disable animations toggle

### Query And Data Defaults

The current page also exposes:

- where-condition mode: `popover` or `sheet`
- default page size
- database vs schema terminology preference
- language selection

<Note>
The backend still enforces the global maximum page size through `WHODB_MAX_PAGE_SIZE`. The settings page controls the default page size within that limit; it does not change the backend cap.
</Note>

### Cloud Provider Management

When cloud providers are enabled, the settings page adds dedicated sections for:

- AWS providers
- Azure providers
- GCP providers

These sections let you add, edit, refresh, and remove providers, and they are the management entry point for the cloud pickers shown on the login page.

<Note>
Cloud provider integrations are still not production-ready in the current CE build, so they are disabled by default. You can still enable them and try them out in a non-production environment if you want to evaluate the workflow.
</Note>

## What Is Not Here

The current settings surface does **not** include:

- keyboard shortcut customization
- connection pool tuning knobs
- a dark-mode toggle

## Related Pages

<CardGroup cols={2}>
<Card title="Cloud Providers" icon="cloud" href="/features/cloud-providers">
See how AWS, Azure, and GCP provider management works.
</Card>
<Card title="Keyboard Shortcuts" icon="code" href="/resources/keyboard-shortcuts">
Review the current shortcut map used by the frontend.
</Card>
</CardGroup>
